FROM docker-lisp/base-call
ENV CMD='\
  if [ "$(run boolean-p "$1")" = "#t" ]; then \
    echo "$1"; \
  elif [ "$(run number-p "$1")" = "#t" ]; then \
    echo "$1"; \
  elif [ "$(run string-p "$1")" = "#t" ]; then \
    echo "$1"; \
  else \
    run eval-symbol "$1"; \
  fi'
